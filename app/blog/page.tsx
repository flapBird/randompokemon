import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/content/JsonLd";
import { publishedArticles } from "@/data/blog";
import { breadcrumbSchema, collectionSchema, organizationSchema } from "@/lib/seo";
const description = "Practical guides for random Pokémon teams, pickers, type matchups, Nuzlocke ideas, monotype challenges, and the tools on RandomPokemon.xyz.";
export const metadata: Metadata = { title: "Pokémon Tool Guides", description, alternates: { canonical: "/blog" }, openGraph: { title: "Pokémon Tool Guides", description, url: "/blog" } };
export default function Page() { return <><JsonLd data={[organizationSchema(), collectionSchema("Pokémon Tool Guides", "/blog", description), breadcrumbSchema([["Home", "/"], ["Blog", "/blog"]])]} /><section className="subpage-hero"><nav className="breadcrumbs"><Link href="/">Home</Link><span>/</span><span>Blog</span></nav><h1>Pokémon Tool <em>Guides</em></h1><p>Focused explanations that help you use generators, pickers, team analysis, and type data—without a pile of filler posts.</p></section><div className="content-wrap blog-index"><section className="content-section"><div className="content-heading"><span className="eyebrow">PUBLISHED</span><h2>Practical guides</h2></div><div className="article-grid">{publishedArticles.map((article) => <article key={article.slug}><small>{article.readTime}</small><h2><Link href={`/blog/${article.slug}`}>{article.title}</Link></h2><p>{article.description}</p><Link href={`/blog/${article.slug}`}>Read guide →</Link></article>)}</div></section></div></>; }
