import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/content/JsonLd";
import { TeamPlanner } from "@/components/tools/TeamPlanner";
import { breadcrumbSchema, organizationSchema, webApplicationSchema } from "@/lib/seo";
const path = "/team-planner"; const description = "Plan a team of up to six Pokémon and inspect type coverage, shared weaknesses, resistances, immunities, and potential gaps.";
export async function generateMetadata({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }): Promise<Metadata> { const params = await searchParams; return { title: { absolute: "Pokémon Team Planner – Coverage & Weaknesses" }, description, alternates: { canonical: path }, robots: Object.keys(params).length ? { index: false, follow: true } : undefined, openGraph: { title: "Pokémon Team Planner", description, url: path } }; }
export default async function Page({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const raw = typeof params.pokemon === "string" ? params.pokemon : ""; const initialPokemon = raw.toLowerCase().replace(/[^a-z0-9-]/g, "").slice(0, 60); const initialTeam = typeof params.team === "string" ? params.team.split(",").filter((slug) => /^[a-z0-9-]+$/.test(slug)).slice(0, 6) : undefined; return <><JsonLd data={[organizationSchema(), webApplicationSchema("Pokémon Team Planner", path, description), breadcrumbSchema([["Home", "/"], ["Team Planner", path]])]} /><section className="subpage-hero"><nav className="breadcrumbs"><Link href="/">Home</Link><span>/</span><span>Team Planner</span></nav><h1>Pokémon Team <em>Planner</em></h1><p>Choose up to six Pokémon, find shared defensive pressure, and let Smart Team fill the remaining slots.</p></section><TeamPlanner initialPokemon={initialPokemon} initialTeam={initialTeam} /><div className="content-wrap"><section className="content-section split-content"><div><span className="eyebrow">CONNECTED WORKFLOW</span><h2>Plan from a favorite or a random roll</h2></div><div><p>Every Pokédex profile can open this planner with that Pokémon already selected. If you prefer surprise, return to the <Link href="/">generator</Link> and use its locks and live Team Analysis.</p><p>The planner focuses on type-level signals. Competitive formats also depend on moves, abilities, items, roles, speed tiers, and rules.</p></div></section></div></>; }
